<p><%= t "sessions.hello" %> <%= @resource.email %>!</p>

<p><%= t "sessions.message_locked_account" %></p>

<p><%= t "sessions.click_link_to_unlock_account" %></p>

<p><%= link_to t("sessions.unlock_account"), unlock_url(@resource, unlock_token: @token) %></p>
